services:
  - type: web
    name: autovaluate-ai
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: Dockerfile
    healthCheckPath: /health
    envVars:
      - key: API_KEY
        generateValue: true
      - key: PORT
        value: 8000
